[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "securities-voice-assistant"
version = "0.1.0"
description = "全地端證券知識型語音客服安全基礎"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
  "alembic>=1.14,<2",
  "fastapi>=0.115,<1",
  "httpx>=0.28,<1",
  "jinja2>=3.1,<4",
  "opencc>=1.4.1,<2",
  "psycopg[binary]>=3.2,<4",
  "pydantic>=2.10,<3",
  "pydantic-settings>=2.7,<3",
  "pypinyin>=0.55,<1",
  "python-multipart>=0.0.20,<1",
  "sqlalchemy>=2.0,<3",
  "uvicorn[standard]>=0.34,<1",
]

[dependency-groups]
dev = [
  "mypy>=1.14,<2",
  "pytest>=8.3,<9",
  "pytest-cov>=6,<7",
  "ruff>=0.9,<1",
]

[tool.hatch.build.targets.wheel]
packages = [
  "services/orchestrator/src/orchestrator",
  "services/knowledge_admin/src/knowledge_admin",
  "packages/answer_contract/src/answer_contract",
  "packages/observability/src/observability",
  "packages/policy/src/policy",
  "packages/retrieval/src/retrieval",
]

[tool.pytest.ini_options]
addopts = "--strict-markers --strict-config"
pythonpath = ["."]
testpaths = ["tests"]

[tool.coverage.run]
omit = ["tests/test_knowledge_postgres.py"]

[tool.ruff]
target-version = "py312"
line-length = 100
src = [
  "services/orchestrator/src",
  "services/knowledge_admin/src",
  "packages/answer_contract/src",
  "packages/observability/src",
  "packages/policy/src",
  "packages/retrieval/src",
  "evals",
  "tests",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.12"
strict = true
files = [
  "services/orchestrator/src",
  "services/knowledge_admin/src",
  "packages/answer_contract/src",
  "packages/observability/src",
  "packages/policy/src",
  "packages/retrieval/src",
  "evals",
  "tests",
]
